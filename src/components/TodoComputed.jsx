export const TodoComputed = () => {
   return (
      <section className=" flex justify-between rounded-b-md bg-white p-4 text-sm text-gray-400">
         <span>5 items left</span>
         <button>Clear Completed</button>
      </section>
   );
};
